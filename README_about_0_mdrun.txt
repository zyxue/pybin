on Mar 28, after the first cycle run finished, check if 0_mdrun.py is still working.

if it's working, then check 0_mdrun.py on mp2, see, if it's working, if it is, then commit.

then add PBS => HOST to conf file on scinet